#include <iostream>
#include <algorithm>
#include <string>

int level(void){
    string S = s.subtr(a, b - a + 1);
    if(S = string())
}

int main(void){
    ios::sync_with_stdio(0);
    cin.tie(0);
    cin >> C;
    string s;
    while(C--){
        cin >> s;
        D[1] = 1;
        while(s){
            
        }
    }
}